services:
  deepfake-detector:
    build: .
    ports:
      - "8501:8501"
    volumes:
      # Mount your dataset and trained model weights without rebuilding the image
      - ./data:/app/data
      - ./models:/app/models
    environment:
      - PYTHONUNBUFFERED=1
